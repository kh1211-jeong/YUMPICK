# DESIGN.md — YUMPICK (그룹 식사 메뉴 결정 서비스)

> 이 파일은 AI 코딩 에이전트가 UI를 만들 때 참조하는 디자인 시스템입니다.
> "AI가 만든 티"를 없애고, 따뜻하고 배려 있는 톤을 일관되게 유지하는 것이 목적입니다.
> 모든 화면은 아래 토큰과 규칙을 따릅니다. 임의의 색·폰트·그림자 사용 금지.

---

## 1. 브랜드 성격 (Design Soul)

- **한 문장**: 눈치 게임 없이, 아무도 상처 안 받고 다 같이 식사 메뉴를 정하는 따뜻한 도구.
- **이름 뜻**: YUMPICK = yum(냠, 맛있음) + pick(고르다, 선택하다). 가볍고 친근한 결정 도구.
- **느낌**: 친근함 · 가벼움 · 배려. 업무 툴처럼 차갑지 않게, 밥 먹으러 가는 설렘.
- **피해야 할 것**: 채도 높은 파란 primary, 시스템 폰트 그대로, 딱딱한 카드, 과한 그림자.
- **무드**: 따뜻한 에디토리얼 레이아웃 + 부드러운 라운드 + 음식의 온기.

---

## 2. 색상 (Color Tokens)

앱 전역에서 아래 CSS 변수만 사용합니다.

```
/* Base */
--bg:            #FBF7F2;   /* 배경 - 아이보리, 따뜻한 종이 느낌 */
--surface:       #FFFFFF;   /* 카드 표면 */
--surface-alt:   #F4ECE3;   /* 서브 카드 / 눌린 상태 */

/* Text */
--text:          #2B2320;   /* 본문 - 완전 블랙 금지, 따뜻한 다크브라운 */
--text-muted:    #8A7E75;   /* 보조 텍스트 */

/* Accent */
--accent:        #E8703A;   /* 메인 - 따뜻한 테라코타/당근색 (식욕 도는 톤) */
--accent-hover:  #D45F2C;
--accent-soft:   #FCE9DE;   /* accent 배경용 옅은 톤 */

/* Secondary accent (투표 강조용) */
--yum:           #F2A93B;   /* 골든 옐로 - '냠' 포인트, 별점·확정 강조 */
--yum-soft:      #FDF0D8;

/* Semantic */
--positive:      #E8703A;   /* 좋아요/선택 - accent와 통일 */
--muted-choice:  #9AA0A6;   /* 비선택/패스 - 회색, 부정 강조 안 함 */
--muted-soft:    #EEF0F1;

/* Line */
--border:        #EBE1D6;   /* 테두리 - 아주 옅게 */
```

- primary는 파랑이 아니라 **테라코타(#E8703A)** 하나로 통일. 버튼·강조·선택 전부 이 색.
- 별점·최종 확정 같은 "기분 좋은 순간"엔 **골든 옐로(#F2A93B)** 를 포인트로.
- 패스·비선택은 빨강 대신 **중립 회색**. 부정적 감정을 강조하지 않는 게 이 앱의 배려 컨셉.

---

## 3. 타이포그래피

```
--font-sans: "Pretendard", -apple-system, system-ui, sans-serif;
```

- 한글은 **Pretendard** 사용 (CDN 로드). 시스템 폰트 그대로 두지 말 것.
- 로고 "yumpick"은 소문자 워드마크, letter-spacing -0.03em, 700.
- 스케일:
  - 화면 타이틀: 24px / 700 / letter-spacing -0.02em
  - 섹션 제목: 18px / 600
  - 본문: 15px / 400 / line-height 1.6
  - 보조: 13px / 400 / --text-muted
  - 버튼: 15px / 600
- 숫자(투표 수, 별점, 시간)는 tabular-nums 적용해 정렬 흔들림 방지.

---

## 4. 레이아웃 / 간격

- 모바일 우선. 최대 폭 **420px** 중앙 정렬 (초대 링크 → 폰으로 열림이 대부분).
- 간격 스케일(4의 배수): 4 · 8 · 12 · 16 · 24 · 32.
- 화면 좌우 패딩: 20px.
- 카드 사이 간격: 12px.
- 하단 탭 바(그룹 / 메뉴추가 / 내정보)는 고정, 최대폭 안에 정렬.
- 요소는 위→아래 세로 흐름. 한 화면 정보 과밀 금지 (선택지 많으면 또 못 정함 = 제품 철학).

---

## 5. 화면별 가이드

### 회원가입 / 로그인
- 입력 필드는 큼직하게(높이 52px), radius 12px, 포커스 시 --accent 테두리.
- 필수: 이름 · 생년월일 · 전화번호. 이메일은 "(선택)" 라벨 명확히.
- 개인정보는 안심 문구 한 줄: "식사 정할 때만 써요. 전화번호는 공개되지 않아요."

### 그룹 목록 / 그룹 추가
- 그룹은 카드 리스트. 유형(커플/팀플/친구)에 맞는 작은 이모지 뱃지.
- "+ 그룹 만들기"는 accent 버튼. 초대 링크는 복사 버튼과 함께 pill 형태로.

### 세션 - 위치 지정
- 카카오맵 임베드, radius 16px로 감싸기. 중심 기준 3km 원을 --accent-soft로 표시.
- "여기 기준으로 찾을게요" 확정 버튼 하단 고정.

### 세션 - 취향 프롬프트 입력
- 큰 텍스트 입력창(placeholder: "오늘 뭐 땡겨요? 예: 매운 건 말고 일식").
- 아래에 참여 현황 "3명 중 2명 입력 완료" 진행 표시.
- [입력 완료] accent 버튼 / [패스] 회색 secondary 버튼 나란히.

### 후보 3곳 & 투표
- 후보 3장 세로 카드. 각 카드: 식당명(18/600) · 카테고리·거리 · **별점(골든 옐로 별)** · 상세 링크.
- 투표 버튼은 카드 하단 pill. 선택 시 --accent-soft 배경 + --accent 테두리 + 통통 모션.
- 상단에 "3곳으로 좁혔어요. 한 곳만 골라주세요" 안내.

### 결과 확정
- 확정 메뉴/식당을 큰 카드로 중앙에. 상단 배려 카피.
  예) "결정! 오늘은 매운 거 빼고 여기예요 🎉"
- 지도(카카오) 카드 아래 radius 16px로 삽입. 골든 옐로 포인트로 축하 느낌.

---

## 6. 컴포넌트 규칙

### 버튼
- radius 14px, 세로 패딩 14px, 주요 액션은 폭 100%.
- Primary: 배경 --accent, 글자 흰색. hover --accent-hover.
- Secondary: 배경 --surface, 테두리 --border, 글자 --text.
- 눌림 시 scale(0.98) + 그림자 축소. 딱딱하지 않게.

### 카드
- 배경 --surface, radius 18px, border 1px --border.
- 그림자: `0 2px 12px rgba(180,150,120,0.10)` — 딱 한 겹, 은은하게. 여러 겹 금지.

### 투표 / 선택 pill
- radius 999px. 선택 전 회색 테두리, 선택 시 --accent-soft 배경 + --accent 테두리.
- 누를 때 가벼운 통통 애니메이션(scale 1→1.1→1).

### 별점
- 골든 옐로(--yum) 별. 반 개 단위 표시. 숫자 병기(예: 4.3).

### 필터 / 태그 칩
- pill, radius 999px, 패딩 8/14. 꺼짐 --surface+--border / 켜짐 --accent-soft+--accent.

### 진행 표시(참여 현황)
- 얇은 프로그레스 바 또는 "n/총원" 텍스트. --accent로 채움.

---

## 7. 톤 & 마이크로카피

- 말투: 친근한 존댓말, 짧게. 명령형보다 권유형.
- 예시:
  - 홈: "오늘 식사, 냠 하고 정해요"
  - 프롬프트: "오늘 뭐 땡겨요?"
  - 대기: "친구들이 고르는 중이에요 (2/4)"
  - 로딩(AI): "취향 모아서 세 곳으로 좁히는 중…"
  - 결과: "결정! 오늘은 여기예요 🎉"
- 이모지는 포인트로만. 한 화면에 1~2개까지.

---

## 8. 애니메이션

- 전환은 부드럽고 짧게: 150~250ms, ease-out.
- 투표 누를 때 · AI가 후보 3곳 띄울 때 · 결과 확정 때만 살짝 통통/페이드 모션.
- 과한 페이지 슬라이드·패럴랙스 금지.

---

## 9. 하지 말 것 (Anti-patterns)

- ❌ 파란색 primary, 보라 그라데이션 → AI 기본값 티.
- ❌ 완전 검정(#000) 텍스트, 순백 위 회색 그림자 남발.
- ❌ 상위 3곳을 다른 후보와 구분 없이 나열 ("AI 추천" 배지로 항상 구분할 것). 4곳 이상은 반드시 "더보기" 뒤에 접어둘 것.
- ❌ 시스템 폰트 방치.
- ❌ 패스/비선택을 빨강·경고색으로 강조 (배려 컨셉 훼손).
- ❌ 전화번호 등 민감정보를 화면에 노출.
